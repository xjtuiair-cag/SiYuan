interface remote_bitbang
remote_bitbang_port 44255

transport select jtag

# 这里定义一个 TAP（DUT 里的 IR 长度你需要改成真实值）
set _CHIPNAME riscv
jtag newtap $_CHIPNAME cpu -irlen 5

target create $_CHIPNAME.cpu riscv -chain-position $_CHIPNAME.cpu 
